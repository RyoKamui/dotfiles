{{ if ne .chezmoi.os "darwin" }}
Library
{{ end }}
# The passphrase-encrypted age key lives in the source checkout only: the
# 00_decrypt script reads it from there. Without this ignore, apply would also
# copy it to ~/key.txt.age.
key.txt.age
# qBittorrent rewrites this runtime-data file (stats, geometry) on every launch —
# deliberately unmanaged so apply never reverts it.
.config/qBittorrent/qBittorrent-data.ini
